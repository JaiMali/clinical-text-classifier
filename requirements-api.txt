# Serving-only deps for the Docker image. The full requirements.txt (training,
# MLflow, tests) is for local dev. Keep these two in sync for shared packages.
torch>=2.2.0
transformers>=4.40.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
